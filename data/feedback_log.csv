import pandas as pd
from sklearn.feature_extraction.text import TfidfVectorizer
from sklearn.linear_model import LogisticRegression
import joblib, os

train = pd.read_csv("data/train.csv")
if os.path.exists("data/feedback_log.csv"):
    fb = pd.read_csv("data/feedback_log.csv")
    fb = fb.rename(columns={"correct_category": "category"})
    train = pd.concat([train, fb[["merchant","description","category"]]])

train["text"] = train["merchant"] + " " + train["description"]

vectorizer = TfidfVectorizer(analyzer="char", ngram_range=(3,5), max_features=20000)
X = vectorizer.fit_transform(train["text"])
y = train["category"]

model = LogisticRegression(max_iter=1000, class_weight="balanced")
model.fit(X, y)

os.makedirs("models", exist_ok=True)
joblib.dump(model, "models/model.pkl")
joblib.dump(vectorizer, "models/vectorizer.pkl")
print("✅ Model retrained with feedback.")
PG&E IL,PG&E PURCHASE,utilities,correct,0.8608054527139624,2025-11-02T08:00:47.014339
APPLE.COM/BILL iCloud Storage,purchase,technology,correct,2025-11-11T06:21:34.351516
